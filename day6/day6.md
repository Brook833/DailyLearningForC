2022.9.14
复习C++primer 2.6自定义数据结构 3.1命名空间的using声明 3.2.1定义和初始化string对象

## 自定义数据结构
最好不要把对象的定义和类的定义放在一起。这么做无异于把两种不同实体的定义混在了一条语句里。

为了确保各个文件中类的定义一致，类通常被定义在头文件中，而且类所在头文件的名字应与类的名字一样。

头文件通常包含那些只能被定义一次的实体，如类，const和constrexpr变量。

头文件保护符:

#ifndef XXX

#define XXX

#endif
## 命名空间的using声明
头文件不应包含using声明（尽量不要使用using声明）

## 标准库类型string
标准库类型string表示可变长的字符序列。
### 定义和初始化string对象
string s1; //默认初始化，s1是一个空字符串

string s2 = s1; //s2是s1的副本

string s3 = "hello"; //s3是该字符串字面值的副本。

string s4(10, 'c'); //s4的内容是10个c。
### 直初始化和拷贝初始化
如果使用等号初始化一个变量，实际上执行的是拷贝初始化，编译器把等号右侧的值拷贝到新创建的对象中去。不使用等号，则是直接初始化。
### 使用getline读取一整行
getline函数的参数是一个输入流和一个string对象，函数从给定的输入流中读入内容，直到遇到换行符为止（换行符也被读取），然后将所读内容存入string对象中（不存在换行符）。

触发getline函数返回的那个换行符实际上被丢弃掉了，得到的string对象并不包含该换行符。