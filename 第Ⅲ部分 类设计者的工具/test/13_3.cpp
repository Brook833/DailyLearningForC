// StrBlob类我们并未添加拷贝构造函数，编译器自动合成一个拷贝构造函数。
// 因此拷贝一个StrBlob时，拷贝其唯一的成员data,使用shared_ptr的拷贝构造函数来进行拷贝，因此其引用计数+1

// StrBlobPtr类我们并未添加拷贝构造函数，编译器自动合成一个拷贝构造函数。
// 拷贝一个StrBlobPtr时，拷贝成员wptr，用weak_ptr的拷贝构造函数进行拷贝，引用计数不变，然后拷贝curr直接进行内存复制。