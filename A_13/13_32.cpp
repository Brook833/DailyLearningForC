/*
不会
默认swap版本(std::swap)，简单交换两个对象(内置类型的对象)
的非静态成员，对HasPtr而言，就是交换string指针ps，引用计数指针use，和整形值i。
可以看出，这种语义是符合预期的————两个HasPtr都指向了原来对方的string，而两者
互换string后，各自的引用计数应该是不变的。因此，默认swap版本已经能正确处理类指针
HasPtr的交换，专用swap版本不会带来更多收益。
*/